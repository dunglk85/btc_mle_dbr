bundle:
  name: btc-mlops

variables:
  catalog:
    description: Unity Catalog name for the selected environment
    default: btc_dev

include:
  - databricks/resources/*.yml

targets:
  dev:
    variables:
      catalog: btc_dev
  prod:
    variables:
      catalog: btc_prd
